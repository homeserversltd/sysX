# sysx-compiler

Schema IR → artifacts (`sysxc`), hashes, and `core.bin` material for sealed boot per `17-sysx-sealed-boot-core-bin.md`.

## Binaries

- Library: `src/lib.rs`
- CLI: `src/main.rs` (`sysxc`)
