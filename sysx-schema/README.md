# sysx-schema

YAML → typed `ServiceSchema` and validation. Paired with `15-schema-contract-v1.md` and `sysx-compiler` IR output.

## Library

`src/lib.rs` — serde types and validation helpers for service definitions.
